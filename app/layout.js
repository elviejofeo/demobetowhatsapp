export const metadata = {
  title: "Aura · Asistente CETEC",
  description: "Demo de atención con IA — Orquesta Supply",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
